from robot import *
import file
import json

fp = "config/robot.json"
file.initConfig(fp)
# config = file.loadConfig(fp)

# bot = Robot(config)

# bot.sendMessage(text="Test, now is 17:55")
# res = bot.getUpdates()
